name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - jupyter
  - matplotlib
  - seaborn
  - dash
  - dash-core-components
  - dash_bootstrap_components
  - dash-bio

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

