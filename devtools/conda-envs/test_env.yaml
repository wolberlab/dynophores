name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - pandas
  - jupyter
  - matplotlib
  - seaborn

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

